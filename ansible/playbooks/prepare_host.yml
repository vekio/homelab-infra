---
- name: Prepare host before hardening
  hosts: "{{ prep_targets | default('docker_hosts') }}"
  become: true

  tasks:
    - name: Update packages and apply upgrades
      ansible.builtin.apt:
        update_cache: true
        upgrade: dist
        autoremove: true
        autoclean: true
