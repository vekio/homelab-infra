---
- name: Verify connectivity
  hosts: "{{ hello_targets | default('all') }}"
  gather_facts: false

  tasks:
    - name: Ping hosts
      ansible.builtin.ping:

    - name: Print hello
      ansible.builtin.debug:
        msg: "Hello from {{ inventory_hostname }}!"
