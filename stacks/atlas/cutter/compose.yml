services:
  cutter:
    image: cutter:latest
    container_name: cutter
    restart: unless-stopped
    networks:
      - proxy
    # ports:
    #   - 8080:8080/tcp # https
    volumes:
      - /srv/appdata/cutter/data:/data:rw
    environment:
      - TZ=${TZ}
      - ROOT_URL=${CUTTER_ROOT_URL}
    labels:
      - traefik.enable=true
      - traefik.http.routers.cutter.entrypoints=http
      - traefik.http.routers.cutter.rule=Host(`cutter.${DOMAIN_DEMOS}`)
      - traefik.http.routers.cutter.service=cutter
      - traefik.http.services.cutter.loadbalancer.server.port=8080

networks:
  proxy:
    external: true
